# Paloma's Orrery -- Module Atlas

Generated: September 22, 2026
Modules: 38 | Functions: 276 | Lines: 19,582

## Classification Coverage

**Undetermined role (7).** No valid `Role:` tag in the module docstring. Not guessed -- add the tag and re-run `add_docstrings.py`, then this file.

- `tools/exhibit_store_editor.py`
- `patch_L322_C2_2_gallery_hovers_and_read_check_20260922.py`
- `tools/store_writer.py`
- `sweep_collapsed_features.py`
- `tools/test_exhibit_store_editor.py`
- `tools/test_mirror_constants.py`
- `tools/test_store_writer.py`

**Undetermined domain (6).** No valid `Domain:` tag.

- `tools/exhibit_store_editor.py`
- `patch_L322_C2_2_gallery_hovers_and_read_check_20260922.py`
- `tools/store_writer.py`
- `sweep_collapsed_features.py`
- `tools/test_exhibit_store_editor.py`
- `tools/test_store_writer.py`

---

## How to Use This Document

Upload this file to a Claude session. Then ask questions:

- "What modules are involved in rendering comets?"
- "I want to add a new spacecraft encounter -- what do I touch?"
- "What imports spacecraft_encounters and what does it import?"
- "Show me the pipeline from GUI click to rendered figure"
- "What would break if I changed constants_new?"

Claude searches this atlas, reads relevant source files,
and explains in context.

For a lighter, human-browsable version of this same scan, see
MODULE_INDEX.md (also generated by this script).

---

## Roles at a Glance

| Role | Count | Description |
|------|-------|-------------|
| rendering | 5 | Builds visual traces, figures, and charts |
| computation | 1 | Math, orbital mechanics, data processing |
| data | 2 | Catalogs, constants, and static datasets |
| cache | 2 | Fetch, store, and retrieve computed data |
| pipeline | 1 | Transforms data between stages (export, conversion, plotting pipelines) |
| utility | 1 | Shared helper functions |
| devtool | 19 | Developer tools (dependency tracing, atlas) |
| undetermined | 7 | No valid Role: tag in the module docstring |

---

## RENDERING: Builds visual traces, figures, and charts

### presentation.py

**Role:** rendering | **Lines:** 161

> presentation.py - Layout, axes, colors, title, and layer ordering.

**Depends on:** models
**Consumed by:** (none -- standalone)

**Public functions:**

- `color_for(slug)` (line 58)
- `order_traces(traces_with_roles)` (line 62) -- traces_with_roles: list of (role, trace_dict). Returns ordered trace
- `data_half_range(traces, buffer)` (line 72) -- Max absolute coordinate across all trace x/y/z, times a buffer (default
- `calculate_grid_dtick(axis_span)` (line 87) -- Clean grid tick spacing (1/2/5 x 10^n) aiming for ~6 gridlines across
- `build_layout(title, axes_spec, data_half_range)` (line 139) -- Build the layout to the orrery's standard dark 3D scene

---

### render_events.py

**Role:** rendering | **Lines:** 20

> render_events.py - Perihelion and event_link markers.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `build_event_traces(name, osc, event_link, legendgroup)` (line 23)

---

### render_objects.py

**Role:** rendering | **Lines:** 63

> render_objects.py - Object markers, center marker, and labels.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `build_object_marker(name, pos_au, color, legendgroup)` (line 32)
- `build_label(name, pos_au, legendgroup)` (line 47)
- `build_center_marker(center_name, is_barycenter)` (line 64)

---

### render_orbits.py

**Role:** rendering | **Lines:** 159

> render_orbits.py - Osculating (and mean-elements) conics.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `solve_kepler(mean_anom_rad, e, tol, itmax)` (line 49) -- Solve M = E - e*sin(E) for eccentric anomaly E (Newton-Raphson).
- `propagate_marker(osc, t_jd)` (line 89) -- Position (AU) at Julian date t_jd, propagated from the snapshot.
- `sweep_conic(osc, n_points)` (line 123) -- Geometric orbit-shape polyline: sweep true anomaly 0..2pi. Returns
- `build_orbit_traces(name, osc, color, n_points, legendgroup, is_mean)` (line 152) -- Build the conic polyline plus its single info marker (project's

---

### render_spacecraft.py

**Role:** rendering | **Lines:** 19

> render_spacecraft.py - Spacecraft full-arc rendering from served positions.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `build_spacecraft_traces(name, served_positions, legendgroup)` (line 22)

---

## COMPUTATION: Math, orbital mechanics, data processing

### resolver.py

**Role:** computation | **Lines:** 216

> resolver.py - SceneSpec -> AssemblyContext.

**Depends on:** cache_reader, catalog, errors, models
**Consumed by:** assemble

**Public functions:**

- `resolve(scene_spec, catalog, cache)` (line 113)

---

## DATA: Catalogs, constants, and static datasets

### catalog.py

**Role:** data | **Lines:** 39

> catalog.py - Object catalog from objects_config.json.

**Depends on:** errors
**Consumed by:** assemble, resolver

**Public functions:**

- `class Catalog` (line 25)

---

### models.py

**Role:** data | **Lines:** 107

> models.py - Data structures for the solar system assembler.

**Depends on:** (none)
**Consumed by:** assemble, fingerprint, presentation, resolver

**Public functions:**

- `class SceneSpec` (line 44) -- The request. Field names track the Phase 1 vocabulary (handoff v0.3
- `class ResolvedObject` (line 81) -- One object, fully resolved against catalog + served cache.
- `class FeatureRequest` (line 97) -- A resolved feature dispatch record. The assembler reports these as
- `class AssemblyContext` (line 107) -- Frozen truth for one assembly. Nothing downstream reinterprets it.
- `class AssemblyResult` (line 120) -- Assembler output. `figure` is a Pyodide-safe Plotly figure dict

---

## CACHE: Fetch, store, and retrieve computed data

### cache_reader.py

**Role:** cache | **Lines:** 60

> cache_reader.py - Reads the served gallery cache.

**Depends on:** errors
**Consumed by:** assemble, resolver

**Public functions:**

- `class CacheReader` (line 32)

---

### gallery_cache_builder.py

**Role:** cache | **Lines:** 1,881

> gallery_cache_builder.py -- standalone nightly builder for the Paloma's Orrery web gallery cache (Phase 1b, ledger L-098). GALLERY repo tool.

**Depends on:** (none)
**Consumed by:** test_gallery_cache_builder_offline

**Public functions:**

- `utc_to_tdb(dt)` (line 130) -- Source: orbit_data_manager.py:41 (orrery 4e2629c). UTC -> TDB for
- `parse_osc_epoch_to_jd(epoch_str)` (line 164) -- Source: export_orbit_cache.py:parse_osc_epoch_to_jd (orrery 4e2629c).
- `resolve_center_slug(center_body)` (line 185) -- Source: export_orbit_cache.py:resolve_center_slug (orrery 4e2629c).
- `fetch_vectors_range(horizons_id, id_type, center, start_dt, stop_dt, step, hkwargs, epoch_jds)` (line 226) -- Fetch a daily range of position vectors in the object's canonical center.
- `fetch_elements(horizons_id, id_type, center, epoch_jd, hkwargs)` (line 273) -- Fetch osculating elements at a JD epoch; return a normalized dict in
- `fetch_solution_tp(name, horizons_id, id_type, hkwargs)` (line 326) -- Solution-level TP from the Horizons raw response header (comets/asteroids
- `measure_trust(obj, osc, warn)` (line 454) -- The two-body error-rate measurement and served `trust` block (manifest
- `guard_monitor(slug, category, points, a, e, k, max_distance_au)` (line 526) -- Return a list of warning dicts (empty if clean). Never rejects; the raw
- `emit_guard_warnings(warnings, center)` (line 562) -- Loud, diagnostic surfacing (F8): a warning nobody reads degrades warn to
- `raw_vectors_path(root, slug)` (line 581)
- `load_raw_vectors(root, slug)` (line 585)
- `save_raw_vectors(root, slug, obj)` (line 593)
- `append_elements_history(root, slug, run_id, els)` (line 600) -- F6: append each night's element set to a per-object JSONL history so the
- `resolve_comet_conic(obj, warn)` (line 620) -- Return (elements_at_perihelion, solution_tp_jd). Adapted from
- `build_osculating_block(els, expected_center_slug, obj, warn)` (line 655) -- Source (schema parity): export_orbit_cache.py:build_osculating_entry
- `build_position_file(root, slug, obj, raw_points)` (line 696) -- Spacecraft position file, km/JD. Source (schema parity):
- `as_of_today_km(raw_points)` (line 730) -- Last (today) raw point converted to km/JD -- the honest 'you are here'.
- `process_object(root, obj, defaults, mode, run_manifest, warn, refresh_spacecraft)` (line 751) -- Fetch + guard + persist raw for one object; return (osc_block_inputs).
- `douglas_peucker(points_dict, tol_au)` (line 863) -- Prune points on near-straight trajectory segments -- 'skip points along
- `last_good_elements(root, slug)` (line 900) -- A-3: most recent osculating element set from the JSONL history.
- `serve_last_good(root, obj, warn, prior_index)` (line 919) -- A-3: on a failed nightly fetch, serve the object's ORBIT from last-good so
- `derive_served(staging, results, defaults, warn)` (line 1058) -- Assemble coverage_index.json (v0.6 schema parity + conic additions) and
- `class ValidationAbort` (line 1152)
- `assert_structural(index, staging)` (line 1156) -- Structural invariants (builder-correctness gates): abort on failure.
- `shrink_gate(staging_root, live_root, warn)` (line 1208) -- Point-count >= 95% per object AND aggregate vs live raw. Data-LOSS gate:
- `class SwapRenameFailed` (line 1258) -- A rename inside the swap was refused on every attempt.
- `atomic_swap_dir(staging, live, run_id, attempts)` (line 1308) -- N1: swap the WHOLE generation directory as one unit -- live -> .prev,
- `restore_after_failed_swap(staging, live, attempts)` (line 1331) -- L-216: put the previous generation back after a swap that could not
- `swap_log_write(live, record, replacing_run_id)` (line 1361) -- L-216, and it comes FIRST: record the swap's outcome OUTSIDE the
- `retried_renames(attempts)` (line 1409) -- Every rename that needed more than one attempt, as (label, tries),
- `print_swap_result(attempts)` (line 1420) -- L-216, Tony's question of 2026-09-21: say how the swap went, on the
- `print_failed_swap_advice(staging, live, state, attempts)` (line 1440) -- L-216: say in plain words what happened and what Tony does next.
- `verify_promoted_data(out_dir, expected_index)` (line 1526) -- L-173/Option 3: confirm the swap actually landed before ever committing.
- `recover_incomplete_swap(out_dir)` (line 1601) -- Run-start crash recovery (A-1 + N1) for the whole-generation swap. If a
- `git_commit(repo_root, data_rel, today_str)` (line 1734) -- Commit + push the served tree. Returns a status dict
- `load_config(path)` (line 1769)
- `features_only_result(obj)` (line 1774) -- A served block for an entry that carries features but no ephemeris.
- `run_build(config, out_dir, mode, only_slug, dry_run, do_commit, refresh_spacecraft)` (line 1801)
- `main(argv)` (line 2040)
- `print_next_steps()` (line 2074) -- The numbered steps after a good hand build.

---

## PIPELINE: Transforms data between stages (export, conversion, plotting pipelines)

### assemble.py

**Role:** pipeline | **Lines:** 83

> assemble.py - Top-level orchestration: scene_spec -> AssemblyResult.

**Depends on:** cache_reader, catalog, models, resolver
**Consumed by:** (none -- standalone)

**Public functions:**

- `assemble_scene(scene_dict, catalog, cache, title)` (line 36)

---

## UTILITY: Shared helper functions

### errors.py

**Role:** utility | **Lines:** 39

> errors.py - Stable exception classes for the solar system assembler.

**Depends on:** (none)
**Consumed by:** cache_reader, catalog, resolver

**Public functions:**

- `class AssemblerError` (line 17) -- Base class for every assembler failure.
- `class FrameRejectionError` (line 21) -- A scene mixes frames the assembler will not silently transform.
- `class UnsupportedInPhase2Error` (line 30) -- A recognized scene-spec field is deliberately not implemented yet.
- `class MissingCachePayloadError` (line 40) -- The served cache has no usable payload for a requested object.
- `class OutOfServedWindowError` (line 48) -- The requested epoch lies outside the cache's served_window bound.
- `class UnknownObjectError` (line 57) -- A requested slug is not in the catalog / served cache.

---

## DEVTOOL: Developer tools (dependency tracing, atlas)

### add_docstrings.py

**Role:** devtool | **Lines:** 1,210

> add_docstrings.py - Two related tools for module-level docstrings.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `detect_line_ending(content_bytes)` (line 681) -- Detect whether file uses CRLF or LF.
- `has_existing_docstring(content_bytes)` (line 688) -- Check if file starts with a docstring (triple quotes).
- `has_leading_comment(content_bytes)` (line 694) -- Check if file starts with # comments (before imports).
- `insert_docstring(content_bytes, docstring_text, line_ending)` (line 700) -- Insert or replace the module docstring.
- `process_module(project_dir, module_name, docstring_text, write)` (line 743) -- Process a single module: read, insert docstring, optionally write.
- `find_docstring_lines(text)` (line 974) -- Locate the module docstring by PARSING, not by scanning for quotes.
- `find_insert_point(lines, start, end)` (line 1001) -- Decide where the tag block goes inside an existing docstring.
- `strip_existing_tags(lines, start, end)` (line 1021) -- Remove an existing Role:/Domain: PAIR already inside the docstring.
- `expand_single_line_docstring(lines, start, quote)` (line 1080) -- Convert """one-liner.""" into an openable multi-line docstring.
- `insert_tags(content_bytes, role, domain, line_ending)` (line 1092) -- Insert or refresh the Role:/Domain: block inside the docstring.
- `iter_tag_targets(project_dir)` (line 1128) -- Yield (relative_path, absolute_path) for every module to sweep.
- `count_credit_lines(content_bytes)` (line 1150) -- How many 'Module updated:' lines the docstring carries.
- `process_module_tags(rel_path, full_path, write)` (line 1166) -- Sweep one module. Returns (status, detail).
- `run_tag_sweep(project_dir, write)` (line 1191) -- Phase 2 sweep across SCAN_PATHS. Preview unless write=True.
- `confirm_write()` (line 1282) -- Ask whether to actually write the just-previewed changes.
- `main()` (line 1296)

---

### check_cache_in_step.py

**Role:** devtool | **Lines:** 176

> check_cache_in_step.py -- the served cache holds the same shells as data/objects_config.json.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `load(root, relative)` (line 71) -- (parsed, problem). problem is None when the file was read.
- `differences(config_side, cache_side, path, found)` (line 84) -- Every path where the two differ, as (path, config value, cache value).
- `short(value)` (line 106)
- `cached_features(cache, holder, slug)` (line 111) -- The features a cache file holds for one object, or None.
- `count_shells(features)` (line 122) -- Named things a visitor can tick: dicts carrying a name, and names lists.
- `check(root)` (line 138) -- (failures, facts). Each failure is (where, message).
- `main()` (line 177)

---

### check_constants_links.py

**Role:** devtool | **Lines:** 248

> check_constants_links.py -- two checks over the gallery's links into the orrery's store, sharing the mirror's own reading so one cause prints one explanation.

**Depends on:** mirror_constants
**Consumed by:** (none -- standalone)

**Public functions:**

- `read(root)` (line 88) -- (config text, export, sha or None, problem or None).
- `mirror_check(root)` (line 109)
- `read_walk(links, export, closed)` (line 154) -- (counts, failures) for the read check. See the module docstring.
- `join_check(root)` (line 207)
- `main(argv)` (line 271)

---

### debug_encke_tp.py

**Role:** devtool | **Lines:** 61

> debug_encke_tp.py -- run the EXACT same live Horizons query gallery_cache_builder.py's fetch_solution_tp() makes for Encke, and print the complete raw response text.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `run_query(label, query_id, id_type, closest_apparition)` (line 42)
- `main()` (line 62)

---

### fingerprint.py

**Role:** devtool | **Lines:** 98

> fingerprint.py - L-080 semantic fingerprint.

**Depends on:** models
**Consumed by:** (none -- standalone)

**Public functions:**

- `fingerprint(artifact_id, result, tolerance)` (line 54)
- `compare(golden, candidate)` (line 92) -- Return a list of human-readable differences. Empty list == match.

---

### gallery_cleanup.py

**Role:** devtool | **Lines:** 184

> gallery_cleanup.py - Remove orphan gallery files not in gallery_metadata.json.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `human(n)` (line 40) -- Format byte count as human-readable string.
- `load_metadata(gallery_dir)` (line 49) -- Load gallery_metadata.json and return the set of referenced filenames.
- `find_referenced_kmz(gallery_dir, kept_json_files)` (line 72) -- Scan kept JSON files for _kmz_handoff references to KMZ filenames.
- `find_orphans(gallery_dir)` (line 102) -- Find orphan JSON, .bak, and KMZ files.
- `main()` (line 152)

---

### gallery_editor.py

**Role:** devtool | **Lines:** 1,073

> Gallery Editor for Paloma's Orrery -- schema version 2 (L-287).

**Depends on:** json_converter
**Consumed by:** (none -- standalone)

**Public functions:**

- `preview_base()` (line 90) -- Local server if it answers within half a second, else the live site.
- `read_json(path)` (line 108) -- Return (data, was_crlf). Raises on missing or invalid file.
- `write_json(path, data, was_crlf)` (line 116) -- Write JSON, 2-space indent, ASCII-escaped, same line endings as read.
- `make_key(label)` (line 126) -- 'Heat Domes' -> 'heat_domes'. ASCII, lowercase, underscores.
- `walk_rooms(rooms, prefix)` (line 137) -- Yield (path, room, depth) for every room, depth-first.
- `find_room(config, path)` (line 145) -- Return (room dict, parent list) for a path, or (None, None).
- `parent_path(path)` (line 161)
- `room_label_chain(config, path)` (line 165) -- 'solar_system/earth/moon' -> ['Solar', 'Earth', 'Moon'] (short names).
- `door_of(config, path)` (line 180)
- `class GalleryEditor` (line 189)

---

### gallery_json_fixer.py

**Role:** devtool | **Lines:** 485

> Gallery JSON Fixer - Update older gallery JSON files for current viewer.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `fix_gallery_json(fig_dict, filename)` (line 147) -- Apply non-destructive fixes to a gallery JSON figure dict.
- `process_file(filepath, dry_run)` (line 362) -- Process a single gallery JSON file.
- `run_batch(gallery_folder, dry_run)` (line 406) -- Process all JSON files in the gallery folder.
- `run_interactive()` (line 488) -- Run with a file browser for selecting individual JSON files.
- `main()` (line 555)

---

### gallery_maintenance_run.py

**Role:** devtool | **Lines:** 1,037

> gallery_maintenance_run.py - one pass over the gallery's generators and checkers.

**Depends on:** (none)
**Consumed by:** test_gallery_cache_builder_offline

**Public functions:**

- `find_repo_root()` (line 318)
- `wrapped(text, width)` (line 327)
- `print_row(state, label, seconds, note)` (line 342)
- `last_meaningful_line(text)` (line 350)
- `line_containing(text, hint)` (line 357)
- `snapshot(path)` (line 364)
- `run_tool(root, runner, argv_tail, cwd)` (line 375) -- Return (state, seconds, output). state is PASS / FAIL / N-A.
- `fetch(url)` (line 411) -- Return (status, body, error). status is None when unreachable.
- `same_content(left, right)` (line 425) -- True when two files differ only in their line endings.
- `check_served(root)` (line 443) -- Fetch each critical-path file from the live site and judge it.
- `unit_of_constant(name)` (line 607) -- The unit a constant's NAME declares, or None.
- `parse_constants(source)` (line 623) -- Top-level numeric assignments, evaluated without executing anything.
- `collect_pointers(config)` (line 686) -- Every dict in objects_config.json carrying an orrery_constant.
- `config_value(entry)` (line 704) -- The number this pointer sits beside, and its unit.
- `store_conversions(constants)` (line 714) -- Unit factors, taken FROM THE STORE rather than typed in here.
- `agreement_depth(left, right)` (line 740) -- How many significant figures two numbers share. Triage, not verdict.
- `judge(name, expression, orrery_value, value, unit, to_au)` (line 750) -- MATCH or a finding, for one pointer.
- `export_rows(root)` (line 829) -- The names the pulled export serves, or an empty set.
- `check_export_freshness(root)` (line 841) -- The gallery's copy of the export is the orrery's, at a named SHA.
- `check_store_drift(root)` (line 878) -- Follow objects_config.json's pointers into the orrery's store.
- `run_generators(root, results)` (line 994)
- `run_checkers(root, checkers, results)` (line 1014)
- `run_live(root, results)` (line 1030)
- `swap_log_note(root)` (line 1046) -- L-216: the most recent cache swap, in one line, on the screen Tony
- `summarize(results, root)` (line 1097)
- `main()` (line 1159)

---

### gallery_studio.py

**Role:** devtool | **Lines:** 5,545

> Gallery Studio - Interactive HTML Export Tool for Paloma's Orrery

**Depends on:** json_converter
**Consumed by:** (none -- standalone)

**Public functions:**

- `class ToolTip` (line 381) -- Hover tooltip for Tkinter widgets.
- `extract_figure_from_html(html_path)` (line 525) -- Extract Plotly figure dict from an HTML file.
- `extract_encyclopedia_for_figure(fig_dict)` (line 727) -- Extract encyclopedia entries for objects present in a Plotly figure.
- `apply_config(fig_dict, config)` (line 891) -- Apply studio configuration to a Plotly figure dict.
- `build_gallery_html(fig_dict, config, title)` (line 2272) -- Build a standalone gallery-ready HTML file from a figure dict.
- `class GalleryStudio` (line 3195) -- Tkinter GUI for configuring and exporting gallery-ready HTML.
- `main()` (line 6198) -- Launch the Gallery Studio.

---

### inspect_staging.py

**Role:** devtool | **Lines:** 131

> inspect_staging.py -- read the results of a gallery_cache_builder.py dry-run and print a plain-language summary (real dates, TP values, point counts), so you can check them without opening the raw JSON files by hand.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `jd_to_calendar(jd)` (line 66)
- `ask_for_staging_path()` (line 74) -- Prompt for the staging folder when none was given on the command line.
- `main()` (line 100)

---

### json_converter.py

**Role:** devtool | **Lines:** 847

> Gallery JSON Converter - Extract Plotly figures from HTML and save as JSON.

**Depends on:** (none)
**Consumed by:** gallery_editor, gallery_studio

**Public functions:**

- `live_scene_urls(repo_root)` (line 217) -- The ?exhibit= values interactive.html serves, read from its source.
- `add_live_card(repo_root, url, title, description, sources)` (line 250) -- Write a schema-v2 INTERACTIVE card into storage (L-288).
- `extract_plotly_json_from_html(html_path)` (line 354) -- Extract Plotly figure JSON from an HTML file.
- `save_figure_json(fig, name, output_folder, category, description, auto_metadata, mode)` (line 595) -- Save a Plotly figure object directly as gallery-ready JSON.
- `convert_html_to_gallery_json(html_path, output_folder, category, description, mode)` (line 649) -- Convert a single HTML visualization to gallery-ready JSON.
- `run_interactive()` (line 830) -- Run the interactive converter with file selection dialog.
- `convert_folder(input_folder, output_folder, category)` (line 960) -- Convert all HTML files in a folder to gallery JSON.

---

### mirror_constants.py

**Role:** devtool | **Lines:** 589

> mirror_constants.py -- write the orrery's exported numbers into data/objects_config.json, and name every link it cannot serve yet.

**Depends on:** (none)
**Consumed by:** check_constants_links, patch_L322_C2_2_gallery_hovers_and_read_check_20260922, store_writer, test_mirror_constants

**Public functions:**

- `class Node` (line 123) -- One parsed value: its Python form, and its span in the text.
- `class Scanner` (line 136)
- `parse_with_spans(text)` (line 262) -- (Node, python value). Raises ValueError on anything it cannot read.
- `collect_links(node, path)` (line 274) -- [(path, entry node)] for every object carrying an orrery_constant.
- `value_slot(entry)` (line 292) -- The node holding this link's number, or None.
- `store_name(entry)` (line 308)
- `class Change` (line 318)
- `class Link` (line 330)
- `render(value)` (line 341) -- A JSON literal, the way this file already writes numbers.
- `spelling_change(entry, export)` (line 346) -- A unit written in another case than the token table's, or None.
- `defined_token(name, export)` (line 370) -- The token this store row defines, or None.
- `same_token(held, token)` (line 383)
- `same_number(held, exported, figures)` (line 387) -- Is the config's number the export's, allowing for rounding?
- `plan(text, export, accept)` (line 406) -- (links, failures). Refused links are named; the rest still write.
- `member_separator(text, slot_start)` (line 503) -- How this object separates its members: same line, or a new one.
- `apply_changes(text, links)` (line 520) -- The config with every change made, bottom-up so offsets hold.
- `report(links, failures, writing)` (line 545)
- `run(root, write, accept)` (line 621)
- `main(argv)` (line 660)

---

### module_atlas.py

**Role:** devtool | **Lines:** 825

> module_atlas.py - Codebase encyclopedia generator for Paloma's Orrery

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `classify_role(module_name, filepath)` (line 145) -- Classify a module's functional role.
- `classify_domain(module_name, filepath)` (line 160) -- Read a module's declared Domain: tag. UNDETERMINED if absent.
- `set_tag_source_dir(project_dir)` (line 191) -- Point name-only lookups at a specific directory.
- `parse_module_tags(filepath)` (line 201) -- Read Role:/Domain: out of a module's docstring.
- `resolve_module_path(module_name)` (line 228) -- Best guess at where module_name's source lives.
- `classify_module(module_name, filepath)` (line 235) -- Classify a module. Returns (role, domain, source).
- `get_module_docstring(filepath)` (line 305) -- Extract module-level docstring, falling back to leading comments.
- `get_public_functions(filepath)` (line 392) -- Extract public function/class names with their docstrings.
- `get_local_imports(filepath, local_modules)` (line 423) -- Extract project-local imports from a Python file.
- `count_lines(filepath)` (line 446) -- Count non-blank lines in a file.
- `iter_module_files(project_dir)` (line 459) -- Yield (module_name, filepath) for every module under SCAN_PATHS.
- `build_dependency_graph(project_dir)` (line 489) -- Build bidirectional dependency graph for all local modules.
- `scan_modules(project_dir)` (line 519) -- Scan every .py file in project_dir once and return the list of
- `regenerate_role_map(modules, source_path)` (line 576) -- Rewrite the ROLE_MAP marker zone in this file from scanned tags.
- `classification_report_lines(modules, heading_level)` (line 643) -- Markdown block naming every module the classifier could not place.
- `generate_atlas(modules, output_path)` (line 693) -- Write MODULE_ATLAS.md from an already-scanned modules list
- `generate_index(modules, output_path)` (line 863) -- Write MODULE_INDEX.md from the SAME already-scanned modules list
- `main()` (line 938)

---

### pull_constants_export.py

**Role:** devtool | **Lines:** 123

> pull_constants_export.py -- copy the orrery's constants export into the gallery, and record the SHA it came from.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `fetch(url)` (line 54) -- (status, body, error). status is None when unreachable.
- `main()` (line 68)

---

### serve_gallery.py

**Role:** devtool | **Lines:** 107

> serve_gallery.py -- serve this repo over http://localhost and open the assembler dev page in a browser.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `port_is_free(port)` (line 59) -- True if nothing is already listening on the port.
- `main()` (line 65)

---

### sweep_report.py

**Role:** devtool | **Lines:** 145

> sweep_report.py - Which cards sweep on a phone in portrait, and why.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `read_figure(path)` (line 51) -- Return (has_scene, has_mapbox, width, height, error).
- `classify(card)` (line 67)
- `main()` (line 112)

---

### test_artifact1_earth.py

**Role:** devtool | **Lines:** 120

> test_artifact1_earth.py - Artifact 1 (Earth alone) end-to-end, CPython side.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `main()` (line 73)

---

### test_gallery_cache_builder_offline.py

**Role:** devtool | **Lines:** 983

> Offline smoke test for gallery_cache_builder.py. Mocks the Horizons fetch layer (no network) and exercises the pipeline: first-build -> derive -> structural validation -> atomic swap, a nightly re-run (shrink gate), and the Guard v2 MONITOR path (warn + keep, never reject). Run: python3 this_file.py

**Depends on:** gallery_cache_builder, gallery_maintenance_run
**Consumed by:** (none -- standalone)

**Public functions:**

- `fake_elements(horizons_id, id_type, center, epoch_jd, hkwargs)` (line 56)
- `fake_vectors(horizons_id, id_type, center, start_dt, stop_dt, step, hkwargs, epoch_jds)` (line 73)
- `fake_solution_tp(name, horizons_id, id_type, hkwargs)` (line 123)
- `install_mocks()` (line 131)
- `main()` (line 138)

---

## UNDETERMINED: No valid Role: tag in the module docstring

### exhibit_store_editor.py

**Role:** undetermined | **Lines:** 574

> exhibit_store_editor.py -- edit the words a visitor reads in the exhibit rooms, and tick what each room opens on. L-334 pieces 3 and 4.

**Depends on:** store_writer
**Consumed by:** test_exhibit_store_editor

**Public functions:**

- `load(root)` (line 91) -- (text, parsed) for the served config.
- `rooms(config_value)` (line 98) -- The slugs this editor offers: the objects carrying an arrival block.
- `numbers_under(member, prefix)` (line 105) -- [(label, value, unit, figures, link)] for the locked rows.
- `word_rows(config_value, slug)` (line 121) -- One row per thing whose words can be edited, in the form's order.
- `arrival_state(config_value, slug)` (line 180) -- (choices, drawn, moon) for the room's opening view.
- `field_value(config_value, path)` (line 193) -- What the file holds at this path, or "" if it holds nothing yet.
- `line_count(text, width)` (line 216) -- How many lines this text takes at that wrap width.
- `measure(text)` (line 229) -- The label shown beside a long field.
- `pending(original, current)` (line 240) -- [(path, new)] for every field whose text the person changed.
- `arrival_changes(config_value, slug, ticked, moon)` (line 256) -- [(path, value)] for the arrival block, or [] if nothing moved.
- `save_message(word_count, arrival_count)` (line 272) -- What the window says after a save. Plain sentences, one routine.
- `verdicts(output)` (line 315) -- [(name, passed, detail)] read off a maintenance run's output.
- `explain(name, passed)` (line 330) -- A sentence about a red verdict, or "" when there is nothing to add.
- `run_checks(root)` (line 339) -- (ok, output). Runs the gallery's offline maintenance run.
- `window_class()` (line 371) -- The window class, built on demand so importing this module costs
- `main(argv)` (line 642)

---

### patch_L322_C2_2_gallery_hovers_and_read_check_20260922.py

**Role:** undetermined | **Lines:** 368

> patch_L322_C2_2_gallery_hovers_and_read_check_20260922.py -- L-322 Stage C2-b.

**Depends on:** mirror_constants, store_writer
**Consumed by:** (none -- standalone)

**Public functions:**

- `class ConfigRefused` (line 99)
- `check_export(export)` (line 137)
- `insert_pointers(text, mirror, slug)` (line 163)
- `run(text, export, mirror, writer)` (line 198)
- `content(raw)` (line 247) -- LF text: line endings are not content.
- `fingerprint(raw)` (line 252)
- `read(root, name)` (line 256)
- `refuse(problems)` (line 262)
- `main()` (line 271)

---

### store_writer.py

**Role:** undetermined | **Lines:** 460

> store_writer.py -- change the WORDS in data/objects_config.json without disturbing anything else. L-334 piece 2.

**Depends on:** mirror_constants
**Consumed by:** exhibit_store_editor, patch_L322_C2_2_gallery_hovers_and_read_check_20260922, test_exhibit_store_editor, test_store_writer

**Public functions:**

- `class WriteRefused` (line 105) -- A change this writer will not make. Nothing has been written.
- `split_path(path)` (line 113) -- ['objects', '1', 'features', ...] from '/objects/1/features/...'.
- `resolve(node, path)` (line 157) -- The Node at this path, with its span in the text.
- `editable_paths(config_value)` (line 200) -- {path: kind} -- every path this writer may write, and no other.
- `drawable_keys(config_value, slug)` (line 242) -- The keys an arrival block's `drawn` list may name, for one room.
- `plan(text, changes)` (line 248) -- [(start, end, piece, path, new)], one per change, unsorted.
- `edit(text, changes)` (line 390) -- The config text with every change made, or WriteRefused.
- `save(path, changes, config)` (line 432) -- Apply the changes to the config on disk. Returns how many.
- `object_index(config_value, slug)` (line 459) -- Which entry in `objects` this room is, or None.
- `shell_fields(config_value, slug)` (line 467) -- [(key, group, path, label)] -- the shells whose WORDS this editor edits.
- `arrival_choices(config_value, slug)` (line 504) -- [(key, label, covers)] -- one row per tick box, the Moon aside.
- `arrival_paths(config_value, slug)` (line 530) -- {'drawn': path, 'moon': path} for one room; missing keys omitted.

---

### sweep_collapsed_features.py

**Role:** undetermined | **Lines:** 228

> DISCOVERY ONLY. Finds every drawable thing in the gallery whose own identity -- its name, its colour, and therefore its link -- is not stored with it in data/objects_config.json. Fixes nothing. Prints a list.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `load(path)` (line 68)
- `style_tables(js)` (line 77) -- Style tables in the renderer that carry a display `name`.
- `declared_names(tables, slug)` (line 101) -- Display names a style table declares for a body, in its own order.
- `main()` (line 114)

---

### test_exhibit_store_editor.py

**Role:** undetermined | **Lines:** 287

> test_exhibit_store_editor.py -- the editor's logic, checked without opening a window. L-334 piece 5, the editor's half.

**Depends on:** exhibit_store_editor, store_writer
**Consumed by:** (none -- standalone)

**Public functions:**

- `check(name, ok, detail)` (line 68)
- `logic_checks()` (line 74)
- `window_walk()` (line 252) -- Open a real window and walk EVERY row of EVERY room.
- `main(argv)` (line 296)

---

### test_mirror_constants.py

**Role:** undetermined | **Lines:** 380

> test_mirror_constants.py -- the mirror writes what it should, refuses what it must, and leaves everything else alone.

**Depends on:** mirror_constants
**Consumed by:** (none -- standalone)

**Public functions:**

- `check(condition, message)` (line 70)
- `export_with(rows, not_exported, tokens)` (line 77)
- `row(value, unit, figures, status, derived)` (line 103)
- `plan_of(config_text, export, accept)` (line 108)
- `ordinary_cases()` (line 152)
- `token_cases()` (line 233)
- `definition_case()` (line 294)
- `shape_cases()` (line 359)
- `report_mode_writes_nothing(root)` (line 399) -- 14: the real config, read and reported, is unchanged on disk.
- `main()` (line 413)

---

### test_store_writer.py

**Role:** undetermined | **Lines:** 451

> test_store_writer.py -- the in-place writer does what it says, and refuses what it must. L-334 piece 5, the writer's half.

**Depends on:** store_writer
**Consumed by:** (none -- standalone)

**Public functions:**

- `check(name, ok, detail)` (line 61)
- `refuses(name, call, expect_in)` (line 67) -- The call must raise WriteRefused, and may have to say something.
- `one_line(before, after, adding)` (line 150) -- Did this change touch one line and nothing else?
- `delta(before, after)` (line 179) -- (lines removed, lines added). A real diff, because an INSERT
- `fixture_checks()` (line 194)
- `real_config_checks()` (line 361)
- `main()` (line 474)

---

## Alphabetical Index

| Module | Role | Lines | Deps | Consumers |
|--------|------|------:|-----:|----------:|
| add_docstrings | devtool | 1,210 | 0 | 0 |
| assemble | pipeline | 83 | 4 | 0 |
| cache_reader | cache | 60 | 1 | 2 |
| catalog | data | 39 | 1 | 2 |
| check_cache_in_step | devtool | 176 | 0 | 0 |
| check_constants_links | devtool | 248 | 1 | 0 |
| debug_encke_tp | devtool | 61 | 0 | 0 |
| errors | utility | 39 | 0 | 3 |
| exhibit_store_editor | undetermined | 574 | 1 | 1 |
| fingerprint | devtool | 98 | 1 | 0 |
| gallery_cache_builder | cache | 1,881 | 0 | 1 |
| gallery_cleanup | devtool | 184 | 0 | 0 |
| gallery_editor | devtool | 1,073 | 1 | 0 |
| gallery_json_fixer | devtool | 485 | 0 | 0 |
| gallery_maintenance_run | devtool | 1,037 | 0 | 1 |
| gallery_studio | devtool | 5,545 | 1 | 0 |
| inspect_staging | devtool | 131 | 0 | 0 |
| json_converter | devtool | 847 | 0 | 2 |
| mirror_constants | devtool | 589 | 0 | 4 |
| models | data | 107 | 0 | 4 |
| module_atlas | devtool | 825 | 0 | 0 |
| patch_L322_C2_2_gallery_hovers_and_read_check_20260922 | undetermined | 368 | 2 | 0 |
| presentation | rendering | 161 | 1 | 0 |
| pull_constants_export | devtool | 123 | 0 | 0 |
| render_events | rendering | 20 | 0 | 0 |
| render_objects | rendering | 63 | 0 | 0 |
| render_orbits | rendering | 159 | 0 | 0 |
| render_spacecraft | rendering | 19 | 0 | 0 |
| resolver | computation | 216 | 4 | 1 |
| serve_gallery | devtool | 107 | 0 | 0 |
| store_writer | undetermined | 460 | 1 | 4 |
| sweep_collapsed_features | undetermined | 228 | 0 | 0 |
| sweep_report | devtool | 145 | 0 | 0 |
| test_artifact1_earth | devtool | 120 | 0 | 0 |
| test_exhibit_store_editor | undetermined | 287 | 2 | 0 |
| test_gallery_cache_builder_offline | devtool | 983 | 2 | 0 |
| test_mirror_constants | undetermined | 380 | 1 | 0 |
| test_store_writer | undetermined | 451 | 1 | 0 |

---

*Generated by module_atlas.py -- Paloma's Orrery Developer Tools*
