# Paloma's Orrery -- Module Atlas

Generated: September 01, 2026
Modules: 29 | Functions: 174 | Lines: 15,207

## Classification Coverage

**Undetermined role (3).** No valid `Role:` tag in the module docstring. Not guessed -- add the tag and re-run `add_docstrings.py`, then this file.

- `diag_L274_why_denied.py`
- `patch_L274_3_readonly_rmtree.py`
- `sweep_collapsed_features.py`

**Undetermined domain (2).** No valid `Domain:` tag.

- `diag_L274_why_denied.py`
- `sweep_collapsed_features.py`

---

## How to Use This Document

Upload this file to a Claude session. Then ask questions:

- "What modules are involved in rendering comets?"
- "I want to add a new spacecraft encounter -- what do I touch?"
- "What imports spacecraft_encounters and what does it import?"
- "Show me the pipeline from GUI click to rendered figure"
- "What would break if I changed constants_new?"

Claude searches this atlas, reads relevant source files,
and explains in context.

For a lighter, human-browsable version of this same scan, see
MODULE_INDEX.md (also generated by this script).

---

## Roles at a Glance

| Role | Count | Description |
|------|-------|-------------|
| rendering | 5 | Builds visual traces, figures, and charts |
| computation | 1 | Math, orbital mechanics, data processing |
| data | 2 | Catalogs, constants, and static datasets |
| cache | 2 | Fetch, store, and retrieve computed data |
| pipeline | 1 | Transforms data between stages (export, conversion, plotting pipelines) |
| utility | 1 | Shared helper functions |
| devtool | 14 | Developer tools (dependency tracing, atlas) |
| undetermined | 3 | No valid Role: tag in the module docstring |

---

## RENDERING: Builds visual traces, figures, and charts

### presentation.py

**Role:** rendering | **Lines:** 161

> presentation.py - Layout, axes, colors, title, and layer ordering.

**Depends on:** models
**Consumed by:** (none -- standalone)

**Public functions:**

- `color_for(slug)` (line 58)
- `order_traces(traces_with_roles)` (line 62) -- traces_with_roles: list of (role, trace_dict). Returns ordered trace
- `data_half_range(traces, buffer)` (line 72) -- Max absolute coordinate across all trace x/y/z, times a buffer (default
- `calculate_grid_dtick(axis_span)` (line 87) -- Clean grid tick spacing (1/2/5 x 10^n) aiming for ~6 gridlines across
- `build_layout(title, axes_spec, data_half_range)` (line 139) -- Build the layout to the orrery's standard dark 3D scene

---

### render_events.py

**Role:** rendering | **Lines:** 20

> render_events.py - Perihelion and event_link markers.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `build_event_traces(name, osc, event_link, legendgroup)` (line 23)

---

### render_objects.py

**Role:** rendering | **Lines:** 63

> render_objects.py - Object markers, center marker, and labels.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `build_object_marker(name, pos_au, color, legendgroup)` (line 32)
- `build_label(name, pos_au, legendgroup)` (line 47)
- `build_center_marker(center_name, is_barycenter)` (line 64)

---

### render_orbits.py

**Role:** rendering | **Lines:** 137

> render_orbits.py - Osculating (and mean-elements) conics.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `solve_kepler(mean_anom_rad, e, tol, itmax)` (line 47) -- Solve M = E - e*sin(E) for eccentric anomaly E (Newton-Raphson).
- `propagate_marker(osc, t_jd)` (line 87) -- Position (AU) at Julian date t_jd, propagated from the snapshot.
- `sweep_conic(osc, n_points)` (line 100) -- Geometric orbit-shape polyline: sweep true anomaly 0..2pi. Returns
- `build_orbit_traces(name, osc, color, n_points, legendgroup, is_mean)` (line 129) -- Build the conic polyline plus its single info marker (project's

---

### render_spacecraft.py

**Role:** rendering | **Lines:** 19

> render_spacecraft.py - Spacecraft full-arc rendering from served positions.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `build_spacecraft_traces(name, served_positions, legendgroup)` (line 22)

---

## COMPUTATION: Math, orbital mechanics, data processing

### resolver.py

**Role:** computation | **Lines:** 216

> resolver.py - SceneSpec -> AssemblyContext.

**Depends on:** cache_reader, catalog, errors, models
**Consumed by:** assemble

**Public functions:**

- `resolve(scene_spec, catalog, cache)` (line 113)

---

## DATA: Catalogs, constants, and static datasets

### catalog.py

**Role:** data | **Lines:** 39

> catalog.py - Object catalog from objects_config.json.

**Depends on:** errors
**Consumed by:** assemble, resolver

**Public functions:**

- `class Catalog` (line 25)

---

### models.py

**Role:** data | **Lines:** 107

> models.py - Data structures for the solar system assembler.

**Depends on:** (none)
**Consumed by:** assemble, fingerprint, presentation, resolver

**Public functions:**

- `class SceneSpec` (line 44) -- The request. Field names track the Phase 1 vocabulary (handoff v0.3
- `class ResolvedObject` (line 81) -- One object, fully resolved against catalog + served cache.
- `class FeatureRequest` (line 97) -- A resolved feature dispatch record. The assembler reports these as
- `class AssemblyContext` (line 107) -- Frozen truth for one assembly. Nothing downstream reinterprets it.
- `class AssemblyResult` (line 120) -- Assembler output. `figure` is a Pyodide-safe Plotly figure dict

---

## CACHE: Fetch, store, and retrieve computed data

### cache_reader.py

**Role:** cache | **Lines:** 60

> cache_reader.py - Reads the served gallery cache.

**Depends on:** errors
**Consumed by:** assemble, resolver

**Public functions:**

- `class CacheReader` (line 32)

---

### gallery_cache_builder.py

**Role:** cache | **Lines:** 1,552

> gallery_cache_builder.py -- standalone nightly builder for the Paloma's Orrery web gallery cache (Phase 1b, ledger L-098). GALLERY repo tool.

**Depends on:** (none)
**Consumed by:** test_gallery_cache_builder_offline

**Public functions:**

- `utc_to_tdb(dt)` (line 119) -- Source: orbit_data_manager.py:41 (orrery 4e2629c). UTC -> TDB for
- `parse_osc_epoch_to_jd(epoch_str)` (line 153) -- Source: export_orbit_cache.py:parse_osc_epoch_to_jd (orrery 4e2629c).
- `resolve_center_slug(center_body)` (line 174) -- Source: export_orbit_cache.py:resolve_center_slug (orrery 4e2629c).
- `fetch_vectors_range(horizons_id, id_type, center, start_dt, stop_dt, step, hkwargs, epoch_jds)` (line 215) -- Fetch a daily range of position vectors in the object's canonical center.
- `fetch_elements(horizons_id, id_type, center, epoch_jd, hkwargs)` (line 262) -- Fetch osculating elements at a JD epoch; return a normalized dict in
- `fetch_solution_tp(name, horizons_id, id_type, hkwargs)` (line 315) -- Solution-level TP from the Horizons raw response header (comets/asteroids
- `measure_trust(obj, osc, warn)` (line 443) -- The two-body error-rate measurement and served `trust` block (manifest
- `guard_monitor(slug, category, points, a, e, k, max_distance_au)` (line 515) -- Return a list of warning dicts (empty if clean). Never rejects; the raw
- `emit_guard_warnings(warnings, center)` (line 551) -- Loud, diagnostic surfacing (F8): a warning nobody reads degrades warn to
- `raw_vectors_path(root, slug)` (line 570)
- `load_raw_vectors(root, slug)` (line 574)
- `save_raw_vectors(root, slug, obj)` (line 582)
- `append_elements_history(root, slug, run_id, els)` (line 589) -- F6: append each night's element set to a per-object JSONL history so the
- `resolve_comet_conic(obj, warn)` (line 609) -- Return (elements_at_perihelion, solution_tp_jd). Adapted from
- `build_osculating_block(els, expected_center_slug, obj, warn)` (line 644) -- Source (schema parity): export_orbit_cache.py:build_osculating_entry
- `build_position_file(root, slug, obj, raw_points)` (line 685) -- Spacecraft position file, km/JD. Source (schema parity):
- `as_of_today_km(raw_points)` (line 719) -- Last (today) raw point converted to km/JD -- the honest 'you are here'.
- `process_object(root, obj, defaults, mode, run_manifest, warn, refresh_spacecraft)` (line 740) -- Fetch + guard + persist raw for one object; return (osc_block_inputs).
- `douglas_peucker(points_dict, tol_au)` (line 852) -- Prune points on near-straight trajectory segments -- 'skip points along
- `last_good_elements(root, slug)` (line 889) -- A-3: most recent osculating element set from the JSONL history.
- `serve_last_good(root, obj, warn, prior_index)` (line 908) -- A-3: on a failed nightly fetch, serve the object's ORBIT from last-good so
- `derive_served(staging, results, defaults, warn)` (line 1028) -- Assemble coverage_index.json (v0.6 schema parity + conic additions) and
- `class ValidationAbort` (line 1122)
- `assert_structural(index, staging)` (line 1126) -- Structural invariants (builder-correctness gates): abort on failure.
- `shrink_gate(staging_root, live_root, warn)` (line 1178) -- Point-count >= 95% per object AND aggregate vs live raw. Data-LOSS gate:
- `atomic_swap_dir(staging, live, run_id)` (line 1208) -- N1: swap the WHOLE generation directory as one unit -- live -> .prev,
- `verify_promoted_data(out_dir, expected_index)` (line 1226) -- L-173/Option 3: confirm the swap actually landed before ever committing.
- `recover_incomplete_swap(out_dir)` (line 1301) -- Run-start crash recovery (A-1 + N1) for the whole-generation swap. If a
- `git_commit(repo_root, data_rel, today_str)` (line 1434) -- Commit + push the served tree. Returns a status dict
- `load_config(path)` (line 1469)
- `features_only_result(obj)` (line 1474) -- A served block for an entry that carries features but no ephemeris.
- `run_build(config, out_dir, mode, only_slug, dry_run, do_commit, refresh_spacecraft)` (line 1501)
- `main(argv)` (line 1712)

---

## PIPELINE: Transforms data between stages (export, conversion, plotting pipelines)

### assemble.py

**Role:** pipeline | **Lines:** 83

> assemble.py - Top-level orchestration: scene_spec -> AssemblyResult.

**Depends on:** cache_reader, catalog, models, resolver
**Consumed by:** (none -- standalone)

**Public functions:**

- `assemble_scene(scene_dict, catalog, cache, title)` (line 36)

---

## UTILITY: Shared helper functions

### errors.py

**Role:** utility | **Lines:** 39

> errors.py - Stable exception classes for the solar system assembler.

**Depends on:** (none)
**Consumed by:** cache_reader, catalog, resolver

**Public functions:**

- `class AssemblerError` (line 17) -- Base class for every assembler failure.
- `class FrameRejectionError` (line 21) -- A scene mixes frames the assembler will not silently transform.
- `class UnsupportedInPhase2Error` (line 30) -- A recognized scene-spec field is deliberately not implemented yet.
- `class MissingCachePayloadError` (line 40) -- The served cache has no usable payload for a requested object.
- `class OutOfServedWindowError` (line 48) -- The requested epoch lies outside the cache's served_window bound.
- `class UnknownObjectError` (line 57) -- A requested slug is not in the catalog / served cache.

---

## DEVTOOL: Developer tools (dependency tracing, atlas)

### add_docstrings.py

**Role:** devtool | **Lines:** 1,210

> add_docstrings.py - Two related tools for module-level docstrings.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `detect_line_ending(content_bytes)` (line 681) -- Detect whether file uses CRLF or LF.
- `has_existing_docstring(content_bytes)` (line 688) -- Check if file starts with a docstring (triple quotes).
- `has_leading_comment(content_bytes)` (line 694) -- Check if file starts with # comments (before imports).
- `insert_docstring(content_bytes, docstring_text, line_ending)` (line 700) -- Insert or replace the module docstring.
- `process_module(project_dir, module_name, docstring_text, write)` (line 743) -- Process a single module: read, insert docstring, optionally write.
- `find_docstring_lines(text)` (line 974) -- Locate the module docstring by PARSING, not by scanning for quotes.
- `find_insert_point(lines, start, end)` (line 1001) -- Decide where the tag block goes inside an existing docstring.
- `strip_existing_tags(lines, start, end)` (line 1021) -- Remove an existing Role:/Domain: PAIR already inside the docstring.
- `expand_single_line_docstring(lines, start, quote)` (line 1080) -- Convert """one-liner.""" into an openable multi-line docstring.
- `insert_tags(content_bytes, role, domain, line_ending)` (line 1092) -- Insert or refresh the Role:/Domain: block inside the docstring.
- `iter_tag_targets(project_dir)` (line 1128) -- Yield (relative_path, absolute_path) for every module to sweep.
- `count_credit_lines(content_bytes)` (line 1150) -- How many 'Module updated:' lines the docstring carries.
- `process_module_tags(rel_path, full_path, write)` (line 1166) -- Sweep one module. Returns (status, detail).
- `run_tag_sweep(project_dir, write)` (line 1191) -- Phase 2 sweep across SCAN_PATHS. Preview unless write=True.
- `confirm_write()` (line 1282) -- Ask whether to actually write the just-previewed changes.
- `main()` (line 1296)

---

### debug_encke_tp.py

**Role:** devtool | **Lines:** 61

> debug_encke_tp.py -- run the EXACT same live Horizons query gallery_cache_builder.py's fetch_solution_tp() makes for Encke, and print the complete raw response text.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `run_query(label, query_id, id_type, closest_apparition)` (line 42)
- `main()` (line 62)

---

### fingerprint.py

**Role:** devtool | **Lines:** 98

> fingerprint.py - L-080 semantic fingerprint.

**Depends on:** models
**Consumed by:** (none -- standalone)

**Public functions:**

- `fingerprint(artifact_id, result, tolerance)` (line 54)
- `compare(golden, candidate)` (line 92) -- Return a list of human-readable differences. Empty list == match.

---

### gallery_cleanup.py

**Role:** devtool | **Lines:** 177

> gallery_cleanup.py - Remove orphan gallery files not in gallery_metadata.json.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `human(n)` (line 37) -- Format byte count as human-readable string.
- `load_metadata(gallery_dir)` (line 46) -- Load gallery_metadata.json and return the set of referenced filenames.
- `find_referenced_kmz(gallery_dir, kept_json_files)` (line 65) -- Scan kept JSON files for _kmz_handoff references to KMZ filenames.
- `find_orphans(gallery_dir)` (line 95) -- Find orphan JSON, .bak, and KMZ files.
- `main()` (line 145)

---

### gallery_editor.py

**Role:** devtool | **Lines:** 1,340

> Gallery Metadata Editor for Paloma's Orrery GUI to edit visualization titles, descriptions, categories, reorder items and categories, and copy/move visualizations within the gallery_metadata.json file.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `load_config(filepath)` (line 40) -- Load gallery_config.json. Returns list of category dicts.
- `save_config(filepath, categories)` (line 50) -- Save gallery_config.json.
- `config_to_map(categories)` (line 57) -- Convert category list to key->label dict.
- `config_to_color_map(categories)` (line 62) -- Convert category list to key->color dict.
- `load_metadata(filepath)` (line 71) -- Load gallery_metadata.json and return the data dict.
- `save_metadata(filepath, data)` (line 77) -- Save gallery_metadata.json with a backup first.
- `get_category_order(vizs, mode_key)` (line 93) -- Derive category display order from JSON sequence for a given mode.
- `make_label_to_key(label)` (line 114) -- Convert a category label to a snake_case key.
- `class GalleryEditor` (line 128)

---

### gallery_json_fixer.py

**Role:** devtool | **Lines:** 485

> Gallery JSON Fixer - Update older gallery JSON files for current viewer.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `fix_gallery_json(fig_dict, filename)` (line 147) -- Apply non-destructive fixes to a gallery JSON figure dict.
- `process_file(filepath, dry_run)` (line 362) -- Process a single gallery JSON file.
- `run_batch(gallery_folder, dry_run)` (line 406) -- Process all JSON files in the gallery folder.
- `run_interactive()` (line 488) -- Run with a file browser for selecting individual JSON files.
- `main()` (line 555)

---

### gallery_maintenance_run.py

**Role:** devtool | **Lines:** 714

> gallery_maintenance_run.py - one pass over the gallery's generators and checkers.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `find_repo_root()` (line 207)
- `wrapped(text, width)` (line 216)
- `print_row(state, label, seconds, note)` (line 231)
- `last_meaningful_line(text)` (line 239)
- `line_containing(text, hint)` (line 246)
- `snapshot(path)` (line 253)
- `run_tool(root, runner, argv_tail, cwd)` (line 264) -- Return (state, seconds, output). state is PASS / FAIL / N-A.
- `fetch(url)` (line 300) -- Return (status, body, error). status is None when unreachable.
- `same_content(left, right)` (line 314) -- True when two files differ only in their line endings.
- `check_served(root)` (line 332) -- Fetch each critical-path file from the live site and judge it.
- `parse_constants(source)` (line 426) -- Top-level numeric assignments, evaluated without executing anything.
- `collect_pointers(config)` (line 489) -- Every dict in objects_config.json carrying an orrery_constant.
- `config_value(entry)` (line 507) -- The number this pointer sits beside, and its unit.
- `store_conversions(constants)` (line 517) -- Unit factors, taken FROM THE STORE rather than typed in here.
- `agreement_depth(left, right)` (line 535) -- How many significant figures two numbers share. Triage, not verdict.
- `judge(name, expression, orrery_value, value, unit, to_au)` (line 545) -- MATCH or a finding, for one pointer.
- `check_store_drift(root)` (line 610) -- Follow objects_config.json's pointers into the orrery's store.
- `run_generators(root, results)` (line 696)
- `run_checkers(root, checkers, results)` (line 716)
- `run_live(root, results)` (line 732)
- `summarize(results)` (line 748)
- `main()` (line 805)

---

### gallery_studio.py

**Role:** devtool | **Lines:** 5,457

> Gallery Studio - Interactive HTML Export Tool for Paloma's Orrery

**Depends on:** json_converter
**Consumed by:** (none -- standalone)

**Public functions:**

- `class ToolTip` (line 376) -- Hover tooltip for Tkinter widgets.
- `extract_figure_from_html(html_path)` (line 520) -- Extract Plotly figure dict from an HTML file.
- `extract_encyclopedia_for_figure(fig_dict)` (line 722) -- Extract encyclopedia entries for objects present in a Plotly figure.
- `apply_config(fig_dict, config)` (line 886) -- Apply studio configuration to a Plotly figure dict.
- `build_gallery_html(fig_dict, config, title)` (line 2256) -- Build a standalone gallery-ready HTML file from a figure dict.
- `class GalleryStudio` (line 3179) -- Tkinter GUI for configuring and exporting gallery-ready HTML.
- `main()` (line 6099) -- Launch the Gallery Studio.

---

### inspect_staging.py

**Role:** devtool | **Lines:** 131

> inspect_staging.py -- read the results of a gallery_cache_builder.py dry-run and print a plain-language summary (real dates, TP values, point counts), so you can check them without opening the raw JSON files by hand.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `jd_to_calendar(jd)` (line 66)
- `ask_for_staging_path()` (line 74) -- Prompt for the staging folder when none was given on the command line.
- `main()` (line 100)

---

### json_converter.py

**Role:** devtool | **Lines:** 624

> Gallery JSON Converter - Extract Plotly figures from HTML and save as JSON.

**Depends on:** (none)
**Consumed by:** gallery_studio

**Public functions:**

- `extract_plotly_json_from_html(html_path)` (line 139) -- Extract Plotly figure JSON from an HTML file.
- `save_figure_json(fig, name, output_folder, category, description, auto_metadata, mode)` (line 380) -- Save a Plotly figure object directly as gallery-ready JSON.
- `convert_html_to_gallery_json(html_path, output_folder, category, description, mode)` (line 434) -- Convert a single HTML visualization to gallery-ready JSON.
- `run_interactive()` (line 594) -- Run the interactive converter with file selection dialog.
- `convert_folder(input_folder, output_folder, category)` (line 717) -- Convert all HTML files in a folder to gallery JSON.

---

### module_atlas.py

**Role:** devtool | **Lines:** 820

> module_atlas.py - Codebase encyclopedia generator for Paloma's Orrery

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `classify_role(module_name, filepath)` (line 140) -- Classify a module's functional role.
- `classify_domain(module_name, filepath)` (line 155) -- Read a module's declared Domain: tag. UNDETERMINED if absent.
- `set_tag_source_dir(project_dir)` (line 186) -- Point name-only lookups at a specific directory.
- `parse_module_tags(filepath)` (line 196) -- Read Role:/Domain: out of a module's docstring.
- `resolve_module_path(module_name)` (line 223) -- Best guess at where module_name's source lives.
- `classify_module(module_name, filepath)` (line 230) -- Classify a module. Returns (role, domain, source).
- `get_module_docstring(filepath)` (line 300) -- Extract module-level docstring, falling back to leading comments.
- `get_public_functions(filepath)` (line 387) -- Extract public function/class names with their docstrings.
- `get_local_imports(filepath, local_modules)` (line 418) -- Extract project-local imports from a Python file.
- `count_lines(filepath)` (line 441) -- Count non-blank lines in a file.
- `iter_module_files(project_dir)` (line 454) -- Yield (module_name, filepath) for every module under SCAN_PATHS.
- `build_dependency_graph(project_dir)` (line 484) -- Build bidirectional dependency graph for all local modules.
- `scan_modules(project_dir)` (line 514) -- Scan every .py file in project_dir once and return the list of
- `regenerate_role_map(modules, source_path)` (line 571) -- Rewrite the ROLE_MAP marker zone in this file from scanned tags.
- `classification_report_lines(modules, heading_level)` (line 638) -- Markdown block naming every module the classifier could not place.
- `generate_atlas(modules, output_path)` (line 688) -- Write MODULE_ATLAS.md from an already-scanned modules list
- `generate_index(modules, output_path)` (line 858) -- Write MODULE_INDEX.md from the SAME already-scanned modules list
- `main()` (line 933)

---

### serve_gallery.py

**Role:** devtool | **Lines:** 107

> serve_gallery.py -- serve this repo over http://localhost and open the assembler dev page in a browser.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `port_is_free(port)` (line 59) -- True if nothing is already listening on the port.
- `main()` (line 65)

---

### test_artifact1_earth.py

**Role:** devtool | **Lines:** 120

> test_artifact1_earth.py - Artifact 1 (Earth alone) end-to-end, CPython side.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `main()` (line 73)

---

### test_gallery_cache_builder_offline.py

**Role:** devtool | **Lines:** 706

> Offline smoke test for gallery_cache_builder.py. Mocks the Horizons fetch layer (no network) and exercises the pipeline: first-build -> derive -> structural validation -> atomic swap, a nightly re-run (shrink gate), and the Guard v2 MONITOR path (warn + keep, never reject). Run: python3 this_file.py

**Depends on:** gallery_cache_builder
**Consumed by:** (none -- standalone)

**Public functions:**

- `fake_elements(horizons_id, id_type, center, epoch_jd, hkwargs)` (line 47)
- `fake_vectors(horizons_id, id_type, center, start_dt, stop_dt, step, hkwargs, epoch_jds)` (line 64)
- `fake_solution_tp(name, horizons_id, id_type, hkwargs)` (line 114)
- `install_mocks()` (line 122)
- `main()` (line 129)

---

## UNDETERMINED: No valid Role: tag in the module docstring

### diag_L274_why_denied.py

**Role:** undetermined | **Lines:** 148

> diag_L274_why_denied.py -- READ ONLY. Why does rmtree get Access Denied?

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `attrs(p)` (line 66) -- Return (list-of-flag-names, error-or-None).
- `report(label, p)` (line 78)
- `main()` (line 89)

---

### patch_L274_3_readonly_rmtree.py

**Role:** undetermined | **Lines:** 285

> patch_L274_3_readonly_rmtree.py -- L-274. Delete past the Windows read-only attribute, at all three deletion sites.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `fail(msg)` (line 215)
- `read_norm(path)` (line 223)
- `main()` (line 239)

---

### sweep_collapsed_features.py

**Role:** undetermined | **Lines:** 228

> DISCOVERY ONLY. Finds every drawable thing in the gallery whose own identity -- its name, its colour, and therefore its link -- is not stored with it in data/objects_config.json. Fixes nothing. Prints a list.

**Depends on:** (none)
**Consumed by:** (none -- standalone)

**Public functions:**

- `load(path)` (line 68)
- `style_tables(js)` (line 77) -- Style tables in the renderer that carry a display `name`.
- `declared_names(tables, slug)` (line 101) -- Display names a style table declares for a body, in its own order.
- `main()` (line 114)

---

## Alphabetical Index

| Module | Role | Lines | Deps | Consumers |
|--------|------|------:|-----:|----------:|
| add_docstrings | devtool | 1,210 | 0 | 0 |
| assemble | pipeline | 83 | 4 | 0 |
| cache_reader | cache | 60 | 1 | 2 |
| catalog | data | 39 | 1 | 2 |
| debug_encke_tp | devtool | 61 | 0 | 0 |
| diag_L274_why_denied | undetermined | 148 | 0 | 0 |
| errors | utility | 39 | 0 | 3 |
| fingerprint | devtool | 98 | 1 | 0 |
| gallery_cache_builder | cache | 1,552 | 0 | 1 |
| gallery_cleanup | devtool | 177 | 0 | 0 |
| gallery_editor | devtool | 1,340 | 0 | 0 |
| gallery_json_fixer | devtool | 485 | 0 | 0 |
| gallery_maintenance_run | devtool | 714 | 0 | 0 |
| gallery_studio | devtool | 5,457 | 1 | 0 |
| inspect_staging | devtool | 131 | 0 | 0 |
| json_converter | devtool | 624 | 0 | 1 |
| models | data | 107 | 0 | 4 |
| module_atlas | devtool | 820 | 0 | 0 |
| patch_L274_3_readonly_rmtree | undetermined | 285 | 0 | 0 |
| presentation | rendering | 161 | 1 | 0 |
| render_events | rendering | 20 | 0 | 0 |
| render_objects | rendering | 63 | 0 | 0 |
| render_orbits | rendering | 137 | 0 | 0 |
| render_spacecraft | rendering | 19 | 0 | 0 |
| resolver | computation | 216 | 4 | 1 |
| serve_gallery | devtool | 107 | 0 | 0 |
| sweep_collapsed_features | undetermined | 228 | 0 | 0 |
| test_artifact1_earth | devtool | 120 | 0 | 0 |
| test_gallery_cache_builder_offline | devtool | 706 | 1 | 0 |

---

*Generated by module_atlas.py -- Paloma's Orrery Developer Tools*
