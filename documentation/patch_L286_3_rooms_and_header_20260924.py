"""patch_L286_3_rooms_and_header_20260924.py -- L-286, the rooms and the header.

RUN COMMAND

    Save this file in the ROOT of the gallery repository
    (tonyquintanilla.github.io), beside index.html. Open it in VS Code
    and click Run.

WHAT IT CHANGES -- index.html only, all-or-nothing

    From the rooms mockup Tony approved on 2026-09-24
    (https://claude.ai/artifact/DLMy8Vu3nV5dNKPU4vFE8X):

    - Each room is its own screen: its title and counts, its own cards,
      then the rooms inside it. Rooms go as deep as the config goes, so
      the Moon is its own room under Earth. A room with no cards stays in
      the list, greyed, "under construction". A door in the lobby opens
      its door's screen.
    - A header across the top of every screen. "Paloma's Orrery" returns
      to the lobby; then the chain of where you are ("Solar: Earth:
      Moon"), each link opening that room. On a card, the card's title
      and its buttons (labels, the i card, links, share, Google Earth)
      sit under the chain, on the desktop as on the phone.
    - The Desktop and Mobile tabs move into the header on every desktop
      screen, the lobby included. None on a phone, and none on a phone
      first opened sideways.
    - The side menu is retired: nothing opens it and it is hidden. Its
      markup stays until a later cleanup.
    - Rooms and cards have their own addresses (#room=<path>, and #<card>
      as before), and opening one adds to the browser's history, so Back
      from a card returns to the room it was opened from.

    NOT in this patch: the dusk-blue wall and the art (L-283), which is
    the next patch; the chain in interactive.html, after that.

TESTED BEFORE DELIVERY, headless, on a copy of the gallery at a2f84a09:
a desktop 1280x860, a phone 390x844, and a phone held sideways. Walked
lobby -> Solar System -> Earth -> Moon -> a card -> Back -> the chain ->
home -> Back; a direct link to #room=solar_system/earth/moon; the tabs
redrawing a room; the links list under its button on a Psyche card; the
turn card's button returning to its room. No page errors. Nothing here
was seen on a real phone.

Built on gallery a2f84a0 (index.html unchanged since 9c61fb21)
at https://github.com/tonylquintanilla/tonyquintanilla.github.io
Recorded on orrery c6cbacae, L-286.

Written September 24, 2026 with Anthropic's Claude Opus 5.5.
"""

import hashlib
import os
import sys

PROBE = "index.html"
PAGE = "index.html"
EXPECTED = 'c5aaad3c88bf78138202f1bb484ea586'
RESULT = '3972682b7ae58cb5fe6498a4b3698629'
EDITS_PAGE = [("the page's Updated stamp", b'         card. The Desktop tab is unchanged. -->\n<html lang="en">', b'         card. The Desktop tab is unchanged.\n     Updated: September 24, 2026 with Anthropic\'s Claude Opus 5.5\n       - Rooms in four levels (L-286, Tony\'s ruling of 2026-09-24 and\n         the mockup he approved). Each room is its own screen: its\n         title and counts, its own cards, then the rooms inside it; a\n         room with no cards stays in the list, greyed, marked "under\n         construction". A door in the lobby opens its door\'s screen.\n         Before, the page kept only two parts of a room path, so the\n         Moon\'s cards listed under Earth.\n       - A header across the top of every screen: "Paloma\'s Orrery"\n         returns to the lobby, then the chain of where you are in short\n         names ("Solar: Earth: Moon"), each link opening that room. On\n         a card, the card\'s title sits under the chain with its buttons\n         (labels, the i card, links, share, Google Earth) beside it,\n         on the desktop as on the phone.\n       - The Desktop and Mobile tabs move into the header, on every\n         desktop screen, the lobby included; none on a phone, and none\n         on a phone first opened sideways.\n       - The side menu is retired. Its markup stays, hidden, until a\n         later cleanup; nothing opens it.\n       - Rooms and cards have their own addresses (#room=<path>, and\n         #<card> as before), and opening one adds to the browser\'s\n         history, so Back returns to the room a card was opened from. -->\n<html lang="en">'), ('styles for the header and the room screens; the side menu hidden', b'        /* ================================================================\n           RESPONSIVE', b"        /* ================================================================\n           THE HEADER AND THE ROOMS (L-286, September 24, 2026)\n           The header replaces the floating menu button. The side menu\n           is retired: its button and the old toolbar are hidden here.\n           ================================================================ */\n        .nav-btn, .viz-toolbar, .overlay-selector, .overlay-backdrop { display: none !important; }\n        .site-header {\n            flex-shrink: 0;\n            display: flex;\n            flex-direction: column;\n            justify-content: center;\n            gap: 2px;\n            min-height: 52px;\n            padding: 6px 20px;\n            background: var(--bg-secondary);\n            border-bottom: 1px solid var(--border);\n            position: relative;\n            z-index: 210;\n        }\n        .site-header-row {\n            display: flex;\n            align-items: center;\n            justify-content: space-between;\n            gap: 12px;\n            min-width: 0;\n        }\n        .site-left { display: flex; align-items: center; gap: 12px; min-width: 0; }\n        .site-home {\n            font-family: 'Cormorant Garamond', Georgia, serif;\n            font-size: 1.15rem;\n            font-weight: 600;\n            color: var(--accent);\n            background: none;\n            border: none;\n            padding: 8px 0;\n            cursor: pointer;\n            white-space: nowrap;\n            flex-shrink: 0;\n        }\n        .site-header.at-lobby .site-home { cursor: default; }\n        .site-sep { width: 1px; height: 18px; background: var(--border); flex-shrink: 0; }\n        .site-header.no-chain .site-sep { display: none; }\n        .site-chain {\n            display: flex;\n            align-items: center;\n            gap: 6px;\n            min-width: 0;\n            font-family: 'DM Sans', sans-serif;\n            font-size: 0.9rem;\n            white-space: nowrap;\n            overflow: hidden;\n        }\n        .site-chain button {\n            background: none;\n            border: none;\n            font: inherit;\n            padding: 8px 0;\n            cursor: pointer;\n        }\n        .site-chain .chain-colon { color: var(--text-dim); }\n        .site-chain .chain-here { color: var(--text-primary); font-weight: 500; }\n        .site-right { display: flex; align-items: center; flex-shrink: 0; }\n        .site-header .mode-toggle { padding: 0; border-bottom: none; }\n        .site-header .mode-btn { flex: none; padding: 6px 14px; }\n        body.is-phone .site-header .mode-toggle { display: none; }\n        .site-title-row { display: none; align-items: center; gap: 8px; min-width: 0; }\n        .site-header.on-card .site-title-row { display: flex; }\n        .site-card-title {\n            flex: 1;\n            min-width: 0;\n            font-family: 'DM Sans', sans-serif;\n            font-size: 0.78rem;\n            color: var(--text-secondary);\n            white-space: nowrap;\n            overflow: hidden;\n            text-overflow: ellipsis;\n        }\n        .site-tools { display: flex; align-items: center; gap: 6px; flex-shrink: 0; }\n        .site-tools > * {\n            position: relative !important;\n            top: auto !important;\n            left: auto !important;\n            right: auto !important;\n            z-index: auto !important;\n        }\n        @media (max-width: 600px) {\n            .site-header { padding: 6px 12px; }\n            .site-left { gap: 10px; }\n            .site-home { font-size: 1.02rem; }\n            .site-chain { font-size: 0.85rem; }\n        }\n\n        .room { width: 100%; max-width: 960px; text-align: left; position: relative; z-index: 1; }\n        .room-title {\n            font-family: 'Cormorant Garamond', Georgia, serif;\n            font-size: 2.2rem;\n            font-weight: 600;\n            line-height: 1.1;\n        }\n        .room-sentence {\n            font-family: 'Cormorant Garamond', Georgia, serif;\n            font-style: italic;\n            font-size: 1.05rem;\n            color: var(--text-secondary);\n            margin-top: 4px;\n        }\n        .room-meta { font-size: 0.8rem; color: var(--text-secondary); margin: 6px 0 24px; }\n        .room-section { margin-bottom: 26px; }\n        .room-grid { display: grid; grid-template-columns: minmax(0, 1fr); gap: 10px; }\n        @media (min-width: 768px) {\n            .room-grid { grid-template-columns: repeat(2, minmax(0, 1fr)); }\n        }\n        @media (min-width: 1025px) {\n            .room-grid { grid-template-columns: repeat(3, minmax(0, 1fr)); }\n        }\n        .room-card { min-height: 0; }\n        .room-card .lobby-card-title { margin-top: 0; font-size: 0.9rem; }\n        .room-card-desc {\n            font-size: 0.78rem;\n            color: var(--text-secondary);\n            line-height: 1.45;\n            margin-top: 4px;\n        }\n        .room-card-meta { font-size: 0.7rem; color: var(--text-dim); margin-top: 6px; }\n        .room-rows .lobby-door-name {\n            font-size: 0.95rem;\n            font-weight: 500;\n            letter-spacing: 0;\n            text-transform: none;\n            color: var(--text-primary);\n        }\n        .room-rows .lobby-door-bar { width: 5px; min-height: 34px; }\n        .room-rows .lobby-door.empty { cursor: default; }\n        .room-rows .lobby-door.empty:hover { background: none; }\n        .room-rows .lobby-door.empty .lobby-door-name { color: var(--text-dim); }\n        .room-special {\n            font-size: 0.62rem;\n            letter-spacing: 0.08em;\n            text-transform: uppercase;\n            color: var(--text-dim);\n            margin-left: 10px;\n        }\n\n        /* ================================================================\n           RESPONSIVE"), ('the header element, first in the page', b'    <div class="app-container">\n        <div class="viz-toolbar" id="vizToolbar"></div>', b'    <div class="app-container">\n        <!-- The header (L-286): home, the chain, the tabs; on a card, its\n             title and buttons. Filled by setHeader(). -->\n        <header class="site-header at-lobby no-chain" id="siteHeader">\n            <div class="site-header-row">\n                <div class="site-left">\n                    <button type="button" class="site-home" id="siteHome">Paloma\'s Orrery</button>\n                    <span class="site-sep"></span>\n                    <nav class="site-chain" id="siteChain" aria-label="Where you are"></nav>\n                </div>\n                <div class="site-right" id="siteRight"></div>\n            </div>\n            <div class="site-title-row">\n                <div class="site-card-title" id="siteCardTitle"></div>\n                <div class="site-tools" id="siteTools"></div>\n            </div>\n        </header>\n        <div class="viz-toolbar" id="vizToolbar"></div>'), ('room records and the room on show', b'        var ROOM_ORDER = {};         // room path -> position in a pre-order walk of the tree (L-286)', b'        var ROOM_ORDER = {};         // room path -> position in a pre-order walk of the tree (L-286)\n        var ROOM_NODES = {};         // room path -> { node, parent } from the same walk (L-286 rooms)\n        var currentRoom = null;      // the room screen on show, or null (L-286 rooms)'), ('the tree walk records each room', b'                    ROOM_LABELS[path] = r.label || r.key;\n', b'                    ROOM_LABELS[path] = r.label || r.key;\n                    ROOM_NODES[path] = { node: r, parent: prefix || null };\n'), ("the tabs and a card's buttons move into the header; a phone shows no tabs", b"            // On mobile: move nav and share buttons into the toolbar\n            // so they sit above the chart instead of floating over it\n            if (window.innerWidth < 1024) {\n                var toolbar = document.getElementById('vizToolbar');\n                if (toolbar) {\n                    toolbar.appendChild(navBtn);\n                    toolbar.appendChild(annToggleBtn);\n                    toolbar.appendChild(encBtn);\n                    toolbar.appendChild(linkBtn);\n                    toolbar.appendChild(shareBtn);\n                }\n            }\n", b"            // The header (L-286, 2026-09-24) holds a card's buttons on\n            // every screen size, where the phone's toolbar and the\n            // desktop's floating buttons held them before; the tabs move\n            // into it from the retired side menu. A phone -- narrow, or a\n            // touch screen whose short side is a phone's, so a phone\n            // first opened sideways too -- shows no tabs.\n            var tools = document.getElementById('siteTools');\n            if (tools) {\n                [document.getElementById('kmz-handoff-btn'), annToggleBtn, encBtn,\n                 linkBtn, shareBtn].forEach(function (el) { if (el) tools.appendChild(el); });\n            }\n            var tabs = document.querySelector('.mode-toggle');\n            var right = document.getElementById('siteRight');\n            if (tabs && right) right.appendChild(tabs);\n            var phoneSideways = !!(window.matchMedia &&\n                window.matchMedia('(pointer: coarse)').matches &&\n                Math.min(screen.width, screen.height) < 768);\n            if (window.innerWidth < 768 || phoneSideways) document.body.classList.add('is-phone');\n            var home = document.getElementById('siteHome');\n            if (home) home.addEventListener('click', function () {\n                if (currentVizId || currentRoom) goHome();\n            });\n"), ('a door opens its screen; the lobby sets the header', b"            welcomeState.innerHTML = html;\n            welcomeState.style.display = 'flex';\n            updateWelcomeCount();\n\n            var doorEls = welcomeState.querySelectorAll('.lobby-door');\n            for (var e = 0; e < doorEls.length; e++) {\n                doorEls[e].addEventListener('click', function () {\n                    openDoor(this.getAttribute('data-door'));\n                });\n            }\n            var cardEls = welcomeState.querySelectorAll('.lobby-card');\n            for (var c = 0; c < cardEls.length; c++) {\n                cardEls[c].addEventListener('click', function () {\n                    loadVisualization(this.getAttribute('data-viz-id'));\n                });\n            }", b"            welcomeState.innerHTML = html;\n            welcomeState.style.display = 'flex';\n            updateWelcomeCount();\n            setHeader(null, null);\n\n            // A door opens its own screen (L-286, 2026-09-24), where it\n            // opened the side menu before.\n            var doorEls = welcomeState.querySelectorAll('.lobby-door');\n            for (var e = 0; e < doorEls.length; e++) {\n                doorEls[e].addEventListener('click', function () {\n                    showRoom(this.getAttribute('data-door'), true);\n                });\n            }\n            var cardEls = welcomeState.querySelectorAll('.lobby-card');\n            for (var c = 0; c < cardEls.length; c++) {\n                cardEls[c].addEventListener('click', function () {\n                    openCard(this.getAttribute('data-viz-id'));\n                });\n            }"), ('setHeader, openCard, leaveCard, showRoom, renderRoom', b'        // ---- Update welcome screen visualization count ----', b'        // ---- Rooms in four levels and the header (L-286, 2026-09-24) ----\n        // A room path is its keys joined by "/" from the door down, as the\n        // editor writes it on each card. ROOM_NODES comes from the walk in\n        // readRoomTree(), so a room the config gains later needs nothing\n        // here.\n        function roomColor(path) {\n            var doorKey = (path || \'\').split(\'/\')[0];\n            return CATEGORY_COLORS[doorKey] || CATEGORY_COLORS.other;\n        }\n        function roomShort(path) {\n            var rec = ROOM_NODES[path];\n            if (!rec) return ROOM_LABELS[path] || path;\n            return rec.node.short || rec.node.label || rec.node.key;\n        }\n        // The rooms from the door down to this one, door first.\n        function roomChain(path) {\n            var chain = [];\n            var p = path;\n            while (p && ROOM_NODES[p]) {\n                chain.unshift(p);\n                p = ROOM_NODES[p].parent;\n            }\n            return chain;\n        }\n        function inSubtree(room, path) {\n            room = room || \'\';\n            return room === path || room.indexOf(path + \'/\') === 0;\n        }\n\n        // One writer for the header. room: the room whose chain shows\n        // (null in the lobby); viz: the open card, or null.\n        function setHeader(room, viz) {\n            var header = document.getElementById(\'siteHeader\');\n            var chainEl = document.getElementById(\'siteChain\');\n            var titleEl = document.getElementById(\'siteCardTitle\');\n            if (!header || !chainEl) return;\n            var chain = room ? roomChain(room) : [];\n            var html = \'\';\n            for (var i = 0; i < chain.length; i++) {\n                var here = (i === chain.length - 1) && !viz;\n                if (i) html += \'<span class="chain-colon">:</span>\';\n                if (here) {\n                    html += \'<span class="chain-here" aria-current="page">\' +\n                            escapeHtml(roomShort(chain[i])) + \'</span>\';\n                } else {\n                    html += \'<button type="button" data-room="\' + escapeHtml(chain[i]) +\n                            \'" style="color: \' + roomColor(chain[i]) + \';">\' +\n                            escapeHtml(roomShort(chain[i])) + \'</button>\';\n                }\n            }\n            chainEl.innerHTML = html;\n            var links = chainEl.querySelectorAll(\'button[data-room]\');\n            for (var j = 0; j < links.length; j++) {\n                links[j].addEventListener(\'click\', function () {\n                    showRoom(this.getAttribute(\'data-room\'), true);\n                });\n            }\n            header.classList.toggle(\'at-lobby\', !room && !viz);\n            header.classList.toggle(\'no-chain\', !chain.length);\n            header.classList.toggle(\'on-card\', !!viz);\n            if (titleEl) titleEl.textContent = viz ? (viz.title || \'Untitled\') : \'\';\n        }\n\n        // Opening a card from a room or the lobby adds it to the browser\'s\n        // history, so Back returns to where it was opened. A live card\n        // leaves the page, and its own page is what Back returns from.\n        function openCard(vizId) {\n            var viz = vizLookup[vizId];\n            if (!viz) return;\n            if (!viz.live) history.pushState(null, \'\', \'#\' + vizId);\n            loadVisualization(vizId);\n        }\n\n        // Take down whatever a card put on screen. goHome() and\n        // showRoom() both call it.\n        function leaveCard() {\n            currentVizId = null;\n            navBtnLabel.innerHTML = \'<span class="nav-label-home">Paloma\\\'s Orrery</span>\';\n            shareBtn.classList.remove(\'visible\');\n            if (typeof Plotly !== \'undefined\' && plotlyGraph.style.display !== \'none\') {\n                Plotly.purge(\'plotly-graph\');\n            }\n            plotlyGraph.style.display = \'none\';\n            plotlyGraph.style.minHeight = \'\';\n            plotlyGraph.style.width = \'\';\n            plotlyGraph.style.height = \'\';\n            plotlyGraph.parentElement.classList.remove(\'swept\');\n            currentAspect = 0;\n            loadingOverlay.classList.remove(\'visible\');\n            if (activeCard) {\n                activeCard.classList.remove(\'active\');\n                activeCard = null;\n            }\n            closeOverlay();\n            dismissInfoCard();\n            zoomControls.classList.remove(\'visible\');\n            panControls.classList.remove(\'visible\');\n            resetStandalone.classList.remove(\'visible\');\n            flytoControls.classList.remove(\'visible\');\n            flytoControls.innerHTML = \'\';\n            annToggleBtn.classList.remove(\'visible\');\n            annStored = null;\n            linkReset();\n            encHideButton();\n            var kmz = document.getElementById(\'kmz-handoff-btn\');\n            if (kmz) kmz.style.display = \'none\';\n        }\n\n        function showRoom(path, push) {\n            if (!ROOM_NODES[path]) { goHome(); return; }\n            leaveCard();\n            currentRoom = path;\n            if (push) history.pushState(null, \'\', \'#room=\' + path);\n            renderRoom(path);\n        }\n\n        function renderRoom(path) {\n            var rec = ROOM_NODES[path];\n            var node = rec.node;\n            var color = roomColor(path);\n            var vizs = (metadata && metadata.visualizations) || [];\n            var shown = [];\n            for (var i = 0; i < vizs.length; i++) {\n                if (inCurrentMode(vizs[i])) shown.push(vizs[i]);\n            }\n            var roomCards = {};\n            for (var s = 0; s < shown.length; s++) roomCards[shown[s].room || \'other\'] = true;\n\n            var here = [], live = 0;\n            for (var h = 0; h < shown.length; h++) {\n                if ((shown[h].room || \'\') === path) {\n                    here.push(shown[h]);\n                    if (shown[h].live) live++;\n                }\n            }\n            here = treeSort(here);\n\n            var kids = node.rooms || [];\n            var kidRows = [], deeper = 0, kidsWithCards = 0, lastKidWithCards = null;\n            for (var k = 0; k < kids.length; k++) {\n                var kp = path + \'/\' + kids[k].key;\n                var n = 0, nLive = 0;\n                for (var m = 0; m < shown.length; m++) {\n                    if (inSubtree(shown[m].room, kp)) {\n                        n++;\n                        if (shown[m].live) nLive++;\n                    }\n                }\n                deeper += n;\n                if (n) { kidsWithCards++; lastKidWithCards = kp; }\n                kidRows.push({ path: kp, node: kids[k], count: n, live: nLive });\n            }\n            var empty = countEmptyRooms({ key: path, rooms: kids }, roomCards);\n\n            var meta = [plural(here.length, \'exhibit\') + (kids.length ? \' here\' : \'\')];\n            if (live) meta.push(plural(live, \'interactive scene\'));\n            if (deeper) {\n                meta.push(deeper + \' more in \' + (kidsWithCards === 1 ?\n                          (ROOM_LABELS[lastKidWithCards] || \'\') : kidsWithCards + \' rooms\'));\n            }\n            if (empty) meta.push(plural(empty, \'room\') + \' under construction\');\n\n            var html = \'<div class="room">\';\n            html += \'<div class="room-title" style="color: \' + color + \';">\' +\n                    escapeHtml(node.label || node.key) + \'</div>\';\n            if (node.sentence) {\n                html += \'<div class="room-sentence">\' + escapeHtml(node.sentence) + \'</div>\';\n            }\n            html += \'<div class="room-meta">\' + escapeHtml(meta.join(\' \\u00b7 \')) + \'</div>\';\n\n            if (here.length) {\n                html += \'<div class="room-section"><div class="lobby-heading">Exhibits here</div>\';\n                html += \'<div class="room-grid">\';\n                for (var c = 0; c < here.length; c++) {\n                    var v = here[c];\n                    html += \'<div class="lobby-card room-card" data-viz-id="\' + escapeHtml(v.id) + \'">\';\n                    html += \'<div class="lobby-card-bar" style="background: \' + color + \';"></div>\';\n                    html += \'<div class="lobby-card-body">\';\n                    html += \'<div class="lobby-card-title">\' + escapeHtml(v.title || \'Untitled\') + \'</div>\';\n                    if (v.description && v.description !== v.title) {\n                        html += \'<div class="room-card-desc">\' + escapeHtml(v.description) + \'</div>\';\n                    }\n                    if (!v.live && v.size_kb) {\n                        html += \'<div class="room-card-meta">\' + escapeHtml(sizeLabel(v)) + \'</div>\';\n                    }\n                    if (v.live) html += \'<div class="viz-card-featured">Interactive</div>\';\n                    html += \'</div></div>\';\n                }\n                html += \'</div></div>\';\n            }\n\n            if (kidRows.length) {\n                html += \'<div class="room-section"><div class="lobby-heading">Rooms</div>\';\n                html += \'<div class="room-rows">\';\n                for (var r = 0; r < kidRows.length; r++) {\n                    var row = kidRows[r];\n                    var rMeta = row.count ? [plural(row.count, \'exhibit\')] : [\'under construction\'];\n                    if (row.live) rMeta.push(plural(row.live, \'interactive scene\'));\n                    html += \'<div class="lobby-door\' + (row.count ? \'\' : \' empty\') +\n                            \'" data-room="\' + escapeHtml(row.path) + \'">\';\n                    html += \'<div class="lobby-door-bar" style="background: \' + color + \';"></div>\';\n                    html += \'<div class="lobby-door-body">\';\n                    html += \'<div class="lobby-door-name">\' + escapeHtml(row.node.label || row.node.key);\n                    if (row.node.special) html += \'<span class="room-special">special exhibit</span>\';\n                    html += \'</div>\';\n                    html += \'<div class="lobby-door-meta">\' + escapeHtml(rMeta.join(\' \\u00b7 \')) + \'</div>\';\n                    html += \'</div>\';\n                    if (row.count) html += \'<div class="lobby-door-chevron">&#9654;</div>\';\n                    html += \'</div>\';\n                }\n                html += \'</div></div>\';\n            }\n            html += \'</div>\';\n\n            welcomeState.innerHTML = html;\n            welcomeState.style.display = \'flex\';\n            welcomeState.scrollTop = 0;\n            setHeader(path, null);\n\n            var cardEls = welcomeState.querySelectorAll(\'.room-card\');\n            for (var e = 0; e < cardEls.length; e++) {\n                cardEls[e].addEventListener(\'click\', function () {\n                    openCard(this.getAttribute(\'data-viz-id\'));\n                });\n            }\n            var rowEls = welcomeState.querySelectorAll(\'.room-rows .lobby-door:not(.empty)\');\n            for (var f = 0; f < rowEls.length; f++) {\n                rowEls[f].addEventListener(\'click\', function () {\n                    showRoom(this.getAttribute(\'data-room\'), true);\n                });\n            }\n        }\n\n        // ---- Update welcome screen visualization count ----'), ('an open card shows its room and title in the header', b'            // Update nav button label to show current viz title\n            navBtnLabel.innerHTML = \'<span class="nav-label-title">\' +\n                escapeHtml(viz.title || \'Untitled\') + \'</span>\';\n', b'            // Update nav button label to show current viz title\n            navBtnLabel.innerHTML = \'<span class="nav-label-title">\' +\n                escapeHtml(viz.title || \'Untitled\') + \'</span>\';\n            // The header shows the card\'s own room and its title (L-286)\n            setHeader(ROOM_NODES[viz.room] ? viz.room : null, viz);\n'), ('switching tabs redraws the room on show', b'                if (!currentVizId) renderLobby(); else updateWelcomeCount();', b'                if (currentVizId) updateWelcomeCount();\n                else if (currentRoom) renderRoom(currentRoom);\n                else renderLobby();'), ('room addresses, and Back to the lobby', b"        function handleHash() {\n            var hash = window.location.hash.replace('#', '');", b"        function handleHash() {\n            var hash = window.location.hash.replace('#', '');\n            // A room's address (L-286): #room=<path>\n            if (hash.indexOf('room=') === 0) {\n                showRoom(decodeURIComponent(hash.slice(5)), false);\n                return;\n            }\n            // Back to the page's own address: the lobby\n            if (!hash) {\n                if (currentVizId || currentRoom) goHome(true);\n                return;\n            }"), ('goHome shares its clean-up with showRoom', b'        function goHome() {\n            // Clear hash\n            history.pushState(null, \'\', window.location.pathname);\n            currentVizId = null;\n\n            // Reset nav button label\n            navBtnLabel.innerHTML = \'<span class="nav-label-home">Paloma\\\'s Orrery</span>\';\n\n            // Hide share button\n            shareBtn.classList.remove(\'visible\');\n\n            // Hide plot, show welcome\n            plotlyGraph.style.display = \'none\';\n            plotlyGraph.style.minHeight = \'\';\n            plotlyGraph.style.width = \'\';\n            plotlyGraph.style.height = \'\';\n            plotlyGraph.parentElement.classList.remove(\'swept\');\n            currentAspect = 0;\n            renderLobby();\n\n            // Deselect active card\n            if (activeCard) {\n                activeCard.classList.remove(\'active\');\n                activeCard = null;\n            }\n\n            // Close overlay\n            closeOverlay();\n            dismissInfoCard();\n            zoomControls.classList.remove(\'visible\');\n            panControls.classList.remove(\'visible\');\n            resetStandalone.classList.remove(\'visible\');\n            flytoControls.classList.remove(\'visible\');\n            flytoControls.innerHTML = \'\';           \n            annToggleBtn.classList.remove(\'visible\');\n            annStored = null;\n            document.getElementById(\'kmz-handoff-btn\').style.display = \'none\'; // NEW\n        }', b"        // noPush is true only when the browser's own Back brought us here;\n        // a click handler passes an event, which must still push.\n        function goHome(noPush) {\n            if (noPush !== true) history.pushState(null, '', window.location.pathname);\n            leaveCard();\n            currentRoom = null;\n            renderLobby();\n        }"), ('the turn card goes back to its room', b'                \'<button type="button" class="turn-card-back" id="turnCardBack">Back to the gallery</button>\' +\n                \'</div>\';\n            welcomeState.style.display = \'flex\';\n            var back = document.getElementById(\'turnCardBack\');\n            if (back) back.addEventListener(\'click\', function () { goHome(); });', b'                \'<button type="button" class="turn-card-back" id="turnCardBack">\' +\n                escapeHtml(ROOM_NODES[viz.room] ? \'Back to \' + (ROOM_LABELS[viz.room] || \'the room\')\n                                                : \'Back to the gallery\') + \'</button>\' +\n                \'</div>\';\n            welcomeState.style.display = \'flex\';\n            var back = document.getElementById(\'turnCardBack\');\n            if (back) back.addEventListener(\'click\', function () {\n                if (ROOM_NODES[viz.room]) showRoom(viz.room, true); else goHome();\n            });'), ('the links list opens under its button', b"                linkDropdown.appendChild(a);\n            });\n            linkDropdown.classList.add('open');", b"                linkDropdown.appendChild(a);\n            });\n            // Under its button in the header (L-286)\n            var lr = linkBtn.getBoundingClientRect();\n            linkDropdown.style.top = Math.round(lr.bottom + 6) + 'px';\n            linkDropdown.style.left = 'auto';\n            linkDropdown.style.right = Math.max(8, Math.round(window.innerWidth - lr.right)) + 'px';\n            linkDropdown.classList.add('open');")]


def fail(msg):
    print("")
    print("FAILURE: %s" % msg)
    print("NOTHING was written.")
    print("Undo is Discard Changes in GitHub Desktop.")
    return 1


def main():
    here = os.path.basename(os.path.abspath(os.getcwd())).lower()
    if not os.path.isfile(PROBE) or here in ("documentation", "gallery", "tools"):
        return fail(
            "%s is not here (%s).\n"
            "         Run this from the GALLERY repo root -- the folder that\n"
            "         holds index.html -- not from gallery/, tools/ or\n"
            "         documentation/, and not from the orrery repo."
            % (PROBE, os.getcwd()))

    raw = open(PAGE, "rb").read()
    was_crlf = b"\r\n" in raw
    content = raw.replace(b"\r\n", b"\n") if was_crlf else raw
    actual = hashlib.md5(content).hexdigest()
    if actual == RESULT:
        return fail("this patch has already been applied: index.html\n"
                    "         already has the rooms and the header.")
    if actual != EXPECTED:
        return fail(
            "BASE MOVED. index.html is not the file this patch was built\n"
            "         against (gallery a2f84a0).\n"
            "         expected %s\n"
            "         found    %s\n"
            "         (Line endings were normalised before comparing, so\n"
            "         CRLF does not explain this -- the content differs.\n"
            "         Tell Claude; do not edit the file by hand.)"
            % (EXPECTED, actual))
    if was_crlf:
        print("note: index.html is CRLF here; compared normalised, written")
        print("      back CRLF exactly as found.")
    out = content
    for label, old, new in EDITS_PAGE:
        count = out.count(old)
        if count != 1:
            return fail("ANCHOR FAIL: expected 1 match, found %d for: %s"
                        % (count, label))
        out = out.replace(old, new)
        print("  ok  %s" % label)
    if any(byt > 127 for byt in out):
        return fail("non-ASCII text would be written; refusing")
    print("  ok  encoding gate: index.html is ASCII after the edit")
    if hashlib.md5(out).hexdigest() != RESULT:
        return fail("the result is not the file this patch was built and\n"
                    "         tested to produce.")
    print("  ok  the result is the file that was tested")

    final = out.replace(b"\n", b"\r\n") if was_crlf else out
    with open(PAGE, "wb") as handle:
        handle.write(final)
    print("  wrote index.html (%d bytes)%s"
          % (len(final), " [CRLF, as found]" if was_crlf else ""))

    print("")
    print("patch applied to 1 file")
    print("")
    print("Stamps updated: the 'Updated' line at the top of index.html.")
    print("")
    print("WHAT TO DO NEXT, in this order:")
    print("")
    print("  1. Move THIS script into documentation/. It has run.")
    print("  2. Run the gallery maintenance run:")
    print("         python gallery_maintenance_run.py")
    print("     Expect every gating checker to pass, as before. None of them")
    print("     opens the gallery page; your eyes in step 5 do.")
    print("  3. In GitHub Desktop the change list should show exactly two")
    print("     files: index.html and this script under documentation/.")
    print("     Commit and push.")
    print("  4. After the push, check what the live site serves:")
    print("         python gallery_maintenance_run.py --live")
    print("  5. On the phone, wait about ten minutes after the push, close the")
    print("     page and open it again, then:")
    print("       - The header shows Paloma's Orrery and no tabs.")
    print("       - Tap Solar System: its own screen, with Earth among the")
    print("         rooms and the empty planets greyed.")
    print("       - Tap Earth, then The Moon: the six Moon cards, and the")
    print("         header reads Solar : Earth : Moon.")
    print("       - Open a Moon card: its title and Share sit under the chain.")
    print("         Swipe back (or the browser's Back): the Moon room again.")
    print("       - Tap Earth in the header, then Paloma's Orrery.")
    print("     On the desktop: the Desktop and Mobile tabs sit at the top")
    print("     right of the lobby and every room; switching tabs redraws the")
    print("     room; a card's Share and other buttons sit in the header.")
    print("  6. Tell Claude the new gallery SHA and what you saw.")
    print("")
    print("TONY-ACTION ROLLUP for this patch:")
    print("  (do)     steps 1 to 6 above.")
    return 0


if __name__ == "__main__":
    sys.exit(main())
